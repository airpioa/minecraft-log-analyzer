/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/minecraft-log-analyzer',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
